{
  "name": "duckdice-casino",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready baseline monorepo for DuckDice casino protocol",
  "workspaces": [
    "packages/*",
    "services/*",
    "contracts"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
